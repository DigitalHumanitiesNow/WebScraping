Our group initially wanted to creating a visual map of the Morrill Building. Our goal was to map the different floors in a mapping tool (for example, StoryMap JS) and then layer in changes to the floor layouts over the years. However, we quickly ran into two major obstacles. StoryMap &#8230;
